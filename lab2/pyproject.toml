[project]
name = "lab2"
version = "0.1.0"
description = "Лабораторна робота 2"
requires-python = ">=3.12"
dependencies = [
    "numpy",
    "pandas",
    "scikit-learn",
    "torch",
    "matplotlib",
]

[tool.uv]
package = false

