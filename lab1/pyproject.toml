[project]
name = "lab1"
version = "0.1.0"
description = "Лабораторна робота 1"
requires-python = ">=3.12"
dependencies = [
    "numpy",
    "pandas",
    "scikit-learn",
    "torch",
]

[tool.uv]
package = false

